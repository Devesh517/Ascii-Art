# ⭐ ASCII Art Generator  
A customizable **ASCII Art Generator** written in Python that converts characters, words, numbers, and symbols into ASCII-styled text.  
It also supports advanced features like background colors (CUI), font scaling, ASCII logos, and saving output to a file.

---

## 📌 Features

✔ Convert **single characters**, **words**, **alphabets**, and **numbers** into ASCII art  
✔ ASCII art **logos** (Heart, Diamond, Smile, etc.)  
✔ Supports **5×5 custom ASCII font**  
✔ **CUI-based menu system** using Python  
✔ **Background color support** (CMD/Terminal)  
✔ **Save last generated ASCII art** to a `.txt` file  
✔ Fully expandable — add more fonts, logos, and colors  
✔ Compatible with Windows CMD (msvcrt used for key-press input)

---

